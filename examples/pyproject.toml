[project]
name = "fpbridge-python-examples"
version = "0.1.0"
description = "Python examples for FPBridge"
requires-python = ">=3.11"
dependencies = [
    "notebook>=7.6.2",
    "urllib3>=2.6,<3",
]

[tool.uv]
package = false
